import styled from "styled-components";

// Styles
const Wrapper = styled.section`
  min-height: 100vh;
  background-color: hsl(228, 33%, 97%);
`;

const Comments = () => {
  return <Wrapper>Comments Section</Wrapper>;
};

export default Comments;
